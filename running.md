Quick commands
--------------
- Install (editable): `python3 -m pip install -e .`
- Run stats: `python3 -m driftkit stats path/to/file.parquet`
- Run compare: `python3 -m driftkit compare ref.parquet cur.parquet`
- Tests: `python3 -m unittest tests.test_smoke`
